// المشروع: متجر إلكتروني (موقع وتطبيق) لبيع ملابس، أحذية، إكسسوارات.

// 🧱 المتطلبات العامة:
1. استخدم HTML وCSS وJavaScript (أو React لو حبيت) لواجهة المستخدم.
2. واجهة جذابة وسهلة الاستخدام باللغة العربية (من اليمين لليسار).
3. العملة المستخدمة في الموقع هي الجنيه المصري (EGP).
4. التصميم متجاوب ويدعم الهواتف.


// 🛍️ المنتجات:
1. المنتجات تشمل (ملابس، أحذية، إكسسوارات...).
2. لكل منتج: 
   - اسم المنتج.
   - صورة رئيسية.
   - صور مختلفة حسب اللون.
   - كود خاص داخلي (لا يظهر للمستخدم).
   - المقاسات (S, M, L, XL مثلاً).
   - الألوان.
3. المنتجات منظمة في أقسام (مثلاً: تيشيرتات، بناطيل، أحذية...).
4. عند اختيار لون المنتج، تتغير الصورة تلقائيًا حسب اللون.
5. يتم تخزين كل المنتجات داخل ملف JSON اسمه products.json بصيغة سهلة.

// مثال لمنتج في JSON:
{
  "id": "P001",
  "name": "تيشيرت قطن",
  "price": 250,
  "category": "تيشيرتات",
  "sizes": ["S", "M", "L"],
  "colors": {
    "أسود": "black-shirt.jpg",
    "أبيض": "white-shirt.jpg"
  },
  "hiddenCode": "TSH-001"
}

// 🛒 السلة والدفع:
1. استخدم localStorage لحفظ المنتجات المضافة للسلة.
2. في السلة، المستخدم يقدر يغير اللون والمقاس والكمية.
3. عند الضغط على "اتمام الطلب"، يتم عرض نموذج به:
   - الاسم.
   - رقم الهاتف.
   - رقم إضافي.
   - المحافظة (اختيارات ثابتة من قائمة مثل القاهرة، الإسكندرية...).
   - المدينة.
   - العنوان بالتفصيل.
   - ملاحظات.
4. عند اختيار المحافظة، يظهر سعر الشحن (ثابت: 65 جنيه).


// 📦 عند تنفيذ الطلب:
1. يتم جمع بيانات الطلب بالكامل (بيانات العميل + تفاصيل المنتجات).
2. ترسل هذه البيانات تلقائيًا عبر fetch إلى بوت تليجرام بدون إشعار المستخدم.

// معلومات بوت تليجرام:
- Token: 7664660691:AAELGdXk_Aky8_lWckjiQgQ1x0Di17QUFLI
- Chat ID: 1448102314

// مثال على إرسال البيانات للـ Telegram:
fetch(`https://api.telegram.org/bot<YOUR_TOKEN>/sendMessage`, {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    chat_id: "<YOUR_CHAT_ID>",
    text: "تفاصيل الطلب هنا..."
  })
})


// 🔧 الإضافة والتعديل على المنتجات:
1. المنتجات مخزنة في ملف products.json.
2. يمكن التعديل على المنتجات بسهولة عبر هذا الملف بإضافة أو حذف منتج.
3. لا حاجة لإعادة برمجة شيء عند تعديل المنتجات.

